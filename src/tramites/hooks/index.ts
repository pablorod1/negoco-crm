// Hooks para lógica de datos
export { useTramitesData } from "./useTramitesData";
export { useTableConfig } from "./useTableConfig";
export { useActiveFilters } from "./useActiveFilters";
export { useProviderFilter } from "./useProviderFilter";

// Re-export hooks existentes para mantener compatibilidad
export { useTableFilters } from "@/core/hooks/use-table-filters";
